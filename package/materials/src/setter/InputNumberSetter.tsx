import { InputNumber } from 'antd'
import styled from '@emotion/styled'
import { typography, space, color, layout, grid } from 'styled-system'

export const InputNumberSetter = styled(InputNumber)(
  layout,
)